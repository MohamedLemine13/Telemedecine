# Angular's @angular/localize and a few peer packages declare exact-version
# peers, which makes npm fail to resolve when the lockfile is even one patch
# behind. legacy-peer-deps tells npm to fall back to npm 6 behaviour — pick the
# best-fit version instead of erroring. Standard workaround in the Angular
# ecosystem.
legacy-peer-deps=true
